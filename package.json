{
  "name": "chart-sampler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "description": "Holiday Lookback Report interactive elements, and a demonstration of which embed strategies survive an LLM crawler.",
  "scripts": {
    "build": "node build.mjs",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "build:all": "npm run build:client && node build.mjs",
    "derive": "node scripts/derive-series.mjs",
    "dev": "npm --prefix client install && npm --prefix client run dev",
    "serve": "node scripts/serve.mjs",
    "share": "npm run build:all && HOST=0.0.0.0 node scripts/serve.mjs",
    "crawl": "node scripts/crawler-view.mjs",
    "test": "node build.mjs && node scripts/smoke.mjs"
  }
}
